{
  "name": "@dexnest/foundation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The small set of things every DexNest module shares: one SQLite connection, one event log, one migration runner, one data boundary, one host vocabulary.",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing.ts"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types --experimental-sqlite --no-warnings test/*.test.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.14"
  }
}
