{
  "name": "@dexnest/today",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Assembles one day's agenda from every source DexNest has, in a shape that does not know which source it came from.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.14"
  }
}
